#+STARTUP: showall hidestars indent
* Contents
- Implementation checklist to tally up all the outstanding work to get this shipped.
- QA checklist for all the test cases that can't readily be automated.
* Implementation Checklist
- [ ] The Product
  - [ ] Video processed into segments
    - [ ] how about recording the final cut of the video?
    - [ ] how about editing it?
    - [ ] Subtitles?
  - [ ] Trailer for videos
    - [ ] how about /writing/ the damn thing?
    - [ ] how about recording it?
    - [ ] how about B-roll?
    - [ ] Upload to YouTube
      - [ ] Description?
      - [ ] Title card?
  - [ ] Player page
- [ ] Assets online and serving
  - [ ] ~media.methodandstructure.com~ configured
    - [ ] Server config
    - [ ] SSL certificate?
  - [ ] [[https://github.com/video-dev/hls.js/][hls.js]] figured out
- [ ] Interfaces designed
  - [ ] Marketing page CTA
  - [ ] "You're already in" pre-checkout interstitial
  - [ ] Seat assignment/confirmation UI
  - [ ] "Hero" error messages (ie those that a user could conceivably encounter in a normal flow)
    - [ ] 503/504 can't connect/timeout to upstream service
    - [ ] 409 for bad form input (maybe? if not handled by JS)
- [X] Changes to [[https://github.com/doriantaylor/rb-forget-passwords][forget-passwords]]
  - [X] ~/me~ resource when logged in
    - [X] (X)HTML variant
    - [X] JSON(-LD) variant
    - [X] returns 401 if not logged in
  - [X] "is ~$PRINCIPAL~ enrolled?" resource
  - [X] "add ~@PRINCIPALS~ to auth list" resource
  - [X] "bypass auth and get cookie for ~$PRINCIPAL~" resource
    - (enroll the principal if not already enrolled)
- [-] ~admit-n~ gem installed
  - [-] Gem pushed to ~rubygems.org~
    - [X] Updated gemspec
    - [ ] Minimal unit test skeleton
  - [-] Code written
    - [ ] Configuration/data hygiene
      - [ ] Type coercions
      - [ ] Configuration parser
      - [ ] Configuration file interface
        - Data source name
        - Daemon host/port
        - JWT key (to interface with ~forget-passwords~ over HTTP)
        - URLs
          - CTA redirect (Stripe checkout URL is automatically computed)
          - "You're already in" interstitial
          - Landing redirect
            - Landing redirect /target/ (defaults to assign/confirm)
          - Assignment/confirmation
            - after-confirmation redirect target (eg "content lobby" which is outside this module)
          - Web hook
        - [ ] Stripe credentials
        - [ ] Anything else?
    - [-] Database adapter
      - [-] ORM
        - [X] harness/class generators
        - [ ] data type inflation/deflation methods
      - [X] database creation
      - [-] Product-specific tailoring
        - [X] Postgres
        - [ ] SQLite
        - [ ] MySQL
    - [X] CLI
      - [X] Install configuration file
    - [-] Install database
      - [ ] Add product
      - [ ] List customers
      - [X] Run daemon (default)
        - Initialize database if not already
    - [ ] Web
      - [ ] CTA → payment processor redirector
        - [ ] "you're already in" case handler
      - [ ] Post-checkout landing → seat assignment
        - [ ] ~GET~ (form UI)
        - [ ] ~POST~ (mutator)
      - [ ] Web hook/asynchronous event target
* Work Cells
1. [X] configuration structure
   - this took six unexpected hours of fiddledicking with indentation macros because something changed out from underneath me
     - also spurred the setting up of rubocop
   - also reminded me why I elected to monkeypatch ~Dry::Types~ instead of trying to use ~Dry::Schema~
2. [X] finish changes to ~forget-passwords~
   - [X] ~/me~ resource that echoes back ~REMOTE_USER~ if present
   - [X] resource for querying if a principal is enrolled
   - [X] resource for bulk enrollments
   - [X] resource for bypassing auth and fetching session cookie
     - enroll principal if not already enrolled
3. [-] Rough in the "happy path"
   - [X] bootstrap from CLI with config file
     - [X] CLI prompt for non-defaultable parameters
     - [X] write config file to target
     - [X] initialize database
   - [ ] web hook
   - [ ] initial redirect to stripe
   - [ ] landing redirect
4. [ ] assign seats and confirm
5. [ ] "you're already in" page
6. [ ] final cut of video
   - [ ] last slides
   - [ ] shoot
   - [ ] cut
7. [ ] trailer
   - [ ] write
   - [ ] shoot
8. [ ] trailer b-roll, cut
9. [ ] put online
* QA Testing Checklist
- Initial CTA (redirect; no content)
  - [ ] Unlisted email is redirected directly to Stripe page.
  - [ ] Stripe checkout session is seeded with recovery link.
    - [ ] E-mailed receipt has link to landing URL (with nonce, checkout ID).
- "You're already in"
  - [ ] Listed email passes through "you're already in" page.
  - [ ] Form gives options to either "Send Sign-In Link" or "Purchase Passes for Others".
    - [ ] (Unless the person is already logged in, then "Send Sign-in Link" should be "Proceed to Lectures").
- Post-payment landing resource (redirect; no content)
  - [ ] Completing a payment (at Stripe) redirects back to landing URL.
  - [ ] Landing resource checks with Stripe to confirm payment (if Web hook has not already been received)
    - [ ] Landing resource retrieves session cookie from auth handler and forwards to client.
    - [ ] Landing resource redirects to seat assignment/confirmation page.
  - [ ] Landing resource errors out appropriately:
    - [ ] 503 for can't verify stripe, fetch cookie, or contact the database.
      - [ ] 500 for database errors besides.
    - [ ] 409 for missing parameters.
    - [ ] 405 for requests other than ~GET~ (it is a redirect target).
- Seat assignment/confirmation resource
  - [ ] Seat assignment/confirmation resource is pre-populated with customer email…
    - [ ] …unless the customer is already assigned a seat by somebody else, then leave it blank.
  - [ ] If the customer is already enrolled (e.g. assigned by another customer or comped by me), the UI should reflect that.
  - [ ] If two or more slots are available, customer can assign a delegate to assign the rest of the seats.
    - [ ] If a delegate is already enrolled, they should not take up a slot.
  - [ ] The UI shouldn't let you assign the same email address to more than one slot.
  - [ ] The UI should warn when you attempt to assign a seat to somebody who already has one through some other means.
  - [ ] ~POST~-ing to "Confirm" should redirect you onward to the designated content URL.
- Web hook (this is actually automatable; returns status codes only, no content)
  - [ ] processes completed checkouts
  - [ ] processes expired checkouts
- Content "lobby"
  - [ ] There is only one video to start with, so we can go straight to it.
